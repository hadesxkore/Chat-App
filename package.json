{
  "name": "chat-app-wrapper",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "chat-app"
  ],
  "scripts": {
    "dev": "cd chat-app && npm run dev",
    "build": "cd chat-app && npm run build",
    "start": "cd chat-app && npm run start"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "next": "^14.0.0"
  }
}
